score.restful <- function(model, data, ...) {
    print("Scoring example python model with data:")
    data
}

score.batch <- function(data_conf, model_conf, model_version, job_id, ...) {
    print("Batch scoring job for R model example.")
}
